#include "main.h"

/**
 * mul - Multiplies two integers
 * @a: First integer
 * @b: Second integer
 *
 * Return: c
 */
int mul(int a, int b)
{
	int c;

	c = a * b;

	if (a < '0' && a >= '0')
	if (b < '0' && b >= '0')
	return (c);
}
