#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - returns the length of a string
 * @s: the string whose length is being read
 *
 * Return: l, the length of the string
 */
int _strlen(char *s)
{
	int l;

	l = strlen(s);
	return (l);
}
