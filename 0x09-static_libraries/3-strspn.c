#include "main.h"

/**
 * _strspn - returns the length of the initial substring pointed to
 * that is made up of only the characters in str2.
 * @s: string
 * @accept: string
 *
 * Return: pointer to length of initial substring
 */
unsigned int _strspn(char *s, char *accept)
{
}
